---
# SPINE:
#   data:
#     device_type: spine
# LEAF:
#   data:
#     device_type: leaf
# VGIS:
#   data:
#     site: VGIS
cisco_ios_xe:
  platform: ios
  connection_options:
    scrapli:
      # platform: 'cisco_xe'
      port: 22
      extras:
        ssh_config_file: True
        transport: ssh2
        auth_strict_key: False
        timeout_transport: 60